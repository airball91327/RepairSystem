@using System.Collections.Generic
@using System.Linq
@using Microsoft.AspNetCore.Http
@using Microsoft.AspNetCore.Http.Authentication
@using EDIS.Models.Identity
@model LoginViewModel
@inject CustomSignInManager SignInManager

@{
    ViewData["Title"] = "登入系統";
}

@*<h2>@ViewData["Title"]</h2>*@

<div style="margin: 40px 20px">
    <div class="row">
        <div class="col-md-12">
            <div class="login center-block">
                <section>
                    <form asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">
                        <h4>登入系統</h4>
                        <hr />
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" name="MailDocId" value="@ViewData["MailDocId"]" />
                        <input type="hidden" name="MailType" value="@ViewData["MailType"]" />

                        <div class="form-group">
                            <label asp-for="UserName"></label>
                            <input asp-for="UserName" class="form-control" />
                            <span asp-validation-for="UserName" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Password"></label>
                            <input asp-for="Password" class="form-control" />
                            <span asp-validation-for="Password" class="text-danger"></span>
                        </div>
                        <div>1.請使用彰基2000的帳號密碼登入系統</div>
                        <div>2.若無彰基2000的帳號，請用外包人員的帳號密碼登入系統</div>
                        <div>3.本系統建議使用Chrome瀏覽器開啟</div>
                        @*<div class="form-group">
            <div>
                <label asp-for="LoginType"></label><br />
                <input asp-for="LoginType" type="radio" value="0" checked />彰基2000
                <input asp-for="LoginType" type="radio" value="1" />外包人員
                <input asp-for="LoginType" type="radio" value="2" />值班人員
            </div>
        </div>*@
                        @*<div class="form-group">
                            <div class="checkbox">
                                <label asp-for="RememberMe">
                                    <input asp-for="RememberMe" />
                                    @Html.DisplayNameFor(m => m.RememberMe)
                                </label>
                            </div>
                        </div>*@
                        <br />
                        <div class="form-group">
                            <button type="submit" class="btn btn-primary">登入系統</button>
                        </div>
                        @*<div class="form-group">
            <p>
                <a asp-action="ForgotPassword">忘記密碼?</a>
            </p>
            <p>
                <a asp-action="Register" asp-route-returnurl="@ViewData[" ReturnUrl"]">申請帳號?</a>
            </p>
        </div>*@
                    </form>
                </section>
            </div>
        </div>

        @*<div class="col-md-6 col-md-offset-2">
                <section>
                    <h4>Use another service to log in.</h4>
                    <hr />
                    @{
                        var loginProviders = (await SignInManager.GetExternalAuthenticationSchemesAsync()).ToList();
                        if (loginProviders.Count == 0)
                        {
                            <div>
                                <p>
                                    There are no external authentication services configured. See <a href="https://go.microsoft.com/fwlink/?LinkID=532715">this article</a>
                                    for details on setting up this ASP.NET application to support logging in via external services.
                                </p>
                            </div>
                        }
                        else
                        {
                            <form asp-action="ExternalLogin" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" class="form-horizontal">
                                <div>
                                    <p>
                                        @foreach (var provider in loginProviders)
                                        {
                                            <button type="submit" class="btn btn-default" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.Name</button>
                                        }
                                    </p>
                                </div>
                            </form>
                        }
                    }
                </section>
            </div>*@
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
