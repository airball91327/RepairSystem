@model X.PagedList.IPagedList<EDIS.Models.Identity.AppUserModel>

@using EDIS.Models.Identity
@using X.PagedList.Mvc.Core;
@using X.PagedList.Mvc.Common;
@inject CustomUserManager UserManager

<link href="~/css/PagedList.css" rel="stylesheet" type="text/css" />

@*<p>
    @if (UserManager.IsInRole(User, "Admin"))
    {
        @Html.ActionLink("新增", "Create", "AppUser", new { Area = "Admin" }, new { @class = "btn btn-primary" })
    }
</p>*@

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().DateCreated)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().LastActivityDate)
        </th>
        <th>功能</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateCreated)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastActivityDate)
            </td>
            <td>
                @Html.ActionLink("修改", "Edit", "AppUser", new { Area = "Admin", id = item.Id }, new { @class = "btn btn-primary" })
                @Html.ActionLink("預覽", "Details", "AppUser", new { Area = "Admin", id = item.Id }, new { @class = "btn btn-primary" })
            </td>
        </tr>
    }

</table>

<div>
    @Html.PagedListPager(Model,
    page => Url.Action("Index", "AppUser", new { Area = "Admin", page }), PagedListRenderOptions.ClassicPlusFirstAndLast)
</div>